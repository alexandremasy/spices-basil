/**
 * Whether or not the value is of type Boolean
 *
 * @param {*} arg The value to check
 * @returns {Boolean}
 */
export default function isBoolean(value){
  return value === true || value === false
}
